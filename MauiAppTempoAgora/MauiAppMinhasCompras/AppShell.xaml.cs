namespace MauiAppMinhasCompras
{
    // Classe AppShell, que herda de Shell, responsável pela navegação e estrutura da aplicação
    public partial class AppShell : Shell
    {
        // Construtor da classe AppShell
        public AppShell()
        {
            InitializeComponent(); // Inicializa os componentes definidos no XAML (associando a interface visual)
        }
    }
}
